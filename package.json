{
  "name": "hausbau-app",
  "version": "0.1.0",
  "private": true,
  "description": "Hausbau App - Rechner und Baustellen-Werkzeuge fuer Bauherren (Android)",
  "scripts": {
    "start": "npx --yes http-server www -p 4300 -c-1",
    "sync": "cap sync android",
    "apk": "cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.4",
    "@capacitor/core": "^7.4.4",
    "@capacitor/filesystem": "^7.1.5",
    "@capacitor/share": "^7.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.4"
  }
}
